在這個影片中, 我們要講關於 矩陣的加法與減法, 還有 如何將矩陣乘上一個數字 也叫做純量乘積. 先從一個例子開始, 給定兩個像這樣的矩陣 假設我要把他們相加起來. 要怎麼做?要怎麼做? 還有, 矩陣的加法代表什麼意思? 看來如果你要 將兩個矩陣相加, 你需要做的只是 把矩陣裡的元素一次一個地加起來. 所以, 相加的結果會是 另一個矩陣, 而這個矩陣的第一個元素是 拿 1 和 4 把他們加起來得到 5. 第二個元素, 我拿2 和2, 加起來得到 4 再來3加0得到 3 持續下去. 我想我要停止換筆的顏色了. 右邊這列是 5, 10, 還有 2. 看起來你只能夠 將兩個相同維度的矩陣相加. 所以這個例子是 一個3 x 2的矩陣 因為它有3 行, 以及 2列, 所以她是3 x 2. 這也是個 3x2 的矩陣 而將她們相加的結果 又是一個 3x2的矩陣. 所以你只能夠 將相同維度的矩陣相加, 而結果會是 另一個矩陣 那個矩陣的維度跟你剛剛加的矩陣的維度一樣 相反的, 如果你要拿這兩個矩陣相加, 這是個3x2的矩陣 3行, 2列, 這個是個2x2的矩陣. 因為這兩個矩陣的維度 不同 這就是一個錯誤 你不能夠加她們相加 而且, 他們的和是沒有被定義的. 這就是矩陣的加法. 再來, 我們講關於將矩陣乘上一個純量 而純量只是一個, 太過漂亮的詞彙, 其實是一個數字, 或是實數. 所以這代表實數 讓我們拿數字 3, 將它乘上這個矩陣. 你應該可以預想這個結果 你就拿矩陣的每一個元素, 將他們乘上 3 一次一個. 所以你知道的, 1 乘上 3 是 3. 2 乘上 3 是 6. 3 乘上 3 是9. 看看 我又要停止換筆的顏色了. 0 乘上 0 是 0. 3 乘上 5 是 15, 3 乘上 1 是 3 所以這個矩陣, 是3 乘上那個矩陣的結果. 再一次注意 這是個 3x2的矩陣 而結果, 是一個相同維度的矩陣. 這是個 3x2 這兩個都是 3x2的矩陣. 順道說ㄧ下 你也可以這樣寫乘法 所以這是 3乘上這個矩陣, 我也可以這樣寫, 1, 0, 2, 5, 3, 1, 好了. 我只是把這個矩陣複製到右邊去. 我可以拿這個新的矩陣乘上 3. 所以不論是 3乘上矩陣, 或是矩陣乘上 3 結果是一樣的, 就是中間這個矩陣. 你也可以拿一個矩陣, 讓她除以一個數字. 看起來 拿這個矩陣, 把它除以 4 其實跟拿 1/4這個數字, 讓它與矩陣相乘, 意思一樣. 4, 0, 6, 3, 那麼你就可以 計算出答案, 而這個答案是 1/4 乘上 4 是 1, 1/4 乘上 0 是0. 1/4 乘上 6 是 3/2, 就是 6/4來的. 1/4 乘上 3 是 3/4. 所以這就是 這個矩陣除以4的結果. 這就得出了結果. 最後呢, 一個稍微複雜一點的例子, 你可以拿這些運算, 然後把它們組合在一起 在這個計算中, 我有3乘上一個向量, 加上一個向量, 再減去另一個向量除以3. 確定你知道這些是什麼. 這個乘法 是一個純量乘積 因為我拿3, 將它乘上一個向量. 而這個, 你知道的, 是另一個純量乘積. 或者可以說是純量除法. 其實只是 向量乘上1/3. 所以如果我們先 計算這兩個運算, 那我們得到的是 等於, 我看看 所以3乘上那個向量是 [3, 12, 6] 加上中間的向量[0, 0, 5], 再減去 向量 [1, 0, 2/3] , 對吧? 再一次, 只是要確認大家知道發生了什麼事. 這個加符號, 是表示矩陣相加對吧? 不過既然這些是向量, 記得, 向量是矩陣的特例嗎? 所以你也可以叫這個為 向量加法. 這裡這個減法符號, 是一個矩陣減法 但因為這其實是個 nx1的矩陣, 其實是個 3x1的矩陣 所以她其實是個向量 而這也是個向量, 我們也可以叫這個向量減法. 好了 最後總結這部分, 這會計算出一個向量 它的第一個元素是 計算 3+0-1 也就是 3-1, 得出 2. 第二個元素計算 12+0-0, 得出 12. 最後第三個元素 計算 6+5 -(2/3) 也就是 11-(2/3) 得出 10 1/3 把括號關起來 這樣結果會得到 一個 3x1 的矩陣, 也可以叫做 3維度的向量, 這個向量是上面這是式子的計算結果. 所以這就是 你如何做矩陣和向量的加減法, 以及純量乘機 目前為止我只講了 如何做矩陣與向量 乘上一個純量 下個影片, 我們會講一個 比較有趣的步驟, 關於如何將兩個矩陣相乘.